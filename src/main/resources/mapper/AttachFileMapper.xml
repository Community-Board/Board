<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.board.mapper.AttachFileMapper">

	<!-- 파일 업로드 -->
	<insert id="uploadFile" parameterType="AttachFileVO" useGeneratedKeys="true" keyProperty="fileNo">
		INSERT INTO board.attach_file
			(file_name, file_src, board_no)
		VALUES
			(#{fileName}, #{fileSrc}, #{boardNo})
	</insert>
	
	<!-- 파일 다운로드 -->
	<select id="selectFile" parameterType="Long" resultType="AttachFileVO">
		SELECT file_no, file_name, file_src, created_at, updated_at, is_deleted, board_no
		FROM board.attach_file
		WHERE file_no = #{fileNo}
	</select>
	
	<!-- 파일 수정 => 파일은 수정이 아닌 삭제!-->
	<update id="updateFile" parameterType="AttachFileVO">
		UPDATE board.attach_file
		SET is_deleted = true
		WHERE file_no = #{fileNo}
	</update>

	<!-- 파일 삭제 is_deleted -->
	<update id="deleteFile" parameterType="AttachFileVO">
		UPDATE board.attach_file
		SET is_deleted = true
		WHERE file_no = #{fileNo}
	</update>
</mapper>